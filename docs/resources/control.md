---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "jupiterone_control Resource - terraform-provider-jupiterone"
subcategory: ""
description: |-
  A custom control.
---

# jupiterone_control (Resource)

A custom control.

## Example Usage

```terraform
resource "jupiterone_control_framework_requirement" "example" {
  framework_id = jupiterone_control_framework.example.id
  title        = "Access Control Policy"
  identifier   = "AC-1"
  priority     = "HIGH"
}

resource "jupiterone_control" "example" {
  name            = "Role-Based Access Control Implementation"
  description     = "Enforces RBAC across all systems to ensure least-privilege access"
  owner           = "security-team@example.com"
  state           = "LIVE"
  identifier      = "CTRL-AC-1"
  catalog         = "Internal Controls"
  remediation     = "Review and update IAM policies to enforce RBAC."
  exception_process = "Exceptions require CISO approval and must be reviewed quarterly."
  requirement_ids = [jupiterone_control_framework_requirement.example.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the control

### Optional

- `catalog` (String) The catalog this control belongs to (e.g. CIS Controls v8)
- `description` (String) Description of the control
- `exception_process` (String) Exception process in markdown format
- `identifier` (String) An identifier for the control used for sorting (e.g. IDAM3.5)
- `owner` (String) The owner of the control, represented by a user email
- `remediation` (String) Remediation steps in markdown format
- `requirement_ids` (List of String) List of control framework requirement IDs to associate with this control
- `resource_group_id` (String) The resource group ID to scope the control to
- `state` (String) The lifecycle state of the control. Must be DRAFT or LIVE on creation; can be transitioned to REVIEW or RETIRED via update.

### Read-Only

- `id` (String) The ID of this resource.


